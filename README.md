# C++ Website

A simple website served using a C++ backend.

## How to Run Locally

1. Install a C++ compiler (`g++`) and `cmake`.
2. Build the project:
   ```bash
   mkdir build
   cd build
   cmake ..
   make
